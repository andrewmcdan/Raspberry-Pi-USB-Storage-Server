[wifi]
interface = wlan0
# auto remembers the last working profile, then tries existing autoconnect profiles.
# Set a specific NetworkManager connection UUID to restrict recovery to one network.
connection_uuid = auto
interval_seconds = 30
connect_timeout_seconds = 25
activation_grace_seconds = 90
reset_after_attempts = 6
